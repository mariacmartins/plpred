{
    "environments": {
        "test": {
            "scripts": {
                "test-setup": "python -m pip install -upgrade pip; pip install -r requirements.txt",
                "test": "python -m pytest"
            }
        }
    }
}